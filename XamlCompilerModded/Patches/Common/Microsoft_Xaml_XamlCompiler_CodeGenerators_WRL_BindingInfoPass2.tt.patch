diff --git a/Microsoft/Xaml/XamlCompiler/CodeGenerators/WRL_BindingInfoPass2.tt b/Microsoft/Xaml/XamlCompiler/CodeGenerators/WRL_BindingInfoPass2.tt
new file mode 100644
index 0000000..da71d6c
--- /dev/null
+++ b/Microsoft/Xaml/XamlCompiler/CodeGenerators/WRL_BindingInfoPass2.tt
@@ -0,0 +1,459 @@
+﻿<#@ template language="C#" inherits="WRL_CodeGenerator<BindingInfoDefinition>" visibility="internal" linePragmas="false"#>
+<#@ import namespace="Microsoft.Xaml.DirectUI" #>
+//------------------------------------------------------------------------------
+// <auto-generated>
+//     This code was generated by a trole.
+//
+//     Changes to this file may cause incorrect behavior and will be lost if
+//     the code is regenerated.
+// </auto-generated>
+//------------------------------------------------------------------------------
+#include "pch.h"
+
+#include "XamlBindingInfo.xaml.g.h"
+
+using namespace Microsoft::WRL;
+
+namespace <#=Colonize(ProjectInfo.RootNamespace)#>
+{
+
+using IDataContextChangedEventArgs = <#=Projection(DUI.NamespaceWindowsUIXaml)#>::IDataContextChangedEventArgs;
+using IDependencyObject = <#=Projection(DUI.NamespaceWindowsUIXaml)#>::IDependencyObject;
+using IDependencyObject2 = <#=Projection(DUI.NamespaceWindowsUIXaml)#>::IDependencyObject2;
+using IDependencyProperty = <#=Projection(DUI.NamespaceWindowsUIXaml)#>::IDependencyProperty;
+using IFrameworkElement = <#=Projection(DUI.NamespaceWindowsUIXaml)#>::IFrameworkElement;
+using INotifyCollectionChanged = <#=Projection(DUI.NamespaceWindowsUIXamlInterop)#>::INotifyCollectionChanged;
+using INotifyPropertyChanged = <#=Projection(DUI.NamespaceWindowsUIXamlData)#>::INotifyPropertyChanged;
+using IPropertyChangedEventArgs = <#=Projection(DUI.NamespaceWindowsUIXamlData)#>::IPropertyChangedEventArgs;
+using INotifyCollectionChangedEventArgs = <#=Projection(DUI.NamespaceWindowsUIXamlInterop)#>::INotifyCollectionChangedEventArgs;
+using IContainerContentChangingEventArgs = <#=Projection(DUI.NamespaceWindowsUIXamlControls)#>::IContainerContentChangingEventArgs;
+<# if (ProjectInfo.IsInputValidationEnabled) { #>
+using INotifyDataErrorInfo = <#=Projection(DUI.NamespaceWindowsUIXamlData)#>::INotifyDataErrorInfo;
+using IDataErrorsChangedEventArgs = <#=Projection(DUI.NamespaceWindowsUIXamlData)#>::IDataErrorsChangedEventArgs;
+<# } #>
+
+// XamlBindings
+
+XamlBindings::XamlBindings()
+{
+}
+
+XamlBindings::~XamlBindings()
+{
+    if (_pBindings)
+    {
+        delete _pBindings;
+        _pBindings = nullptr;
+    }
+}
+
+HRESULT XamlBindings::RuntimeClassInitialize(IXamlBindings* pBindings)
+{
+    _pBindings = pBindings;
+    return S_OK;
+}
+
+HRESULT XamlBindings::Initialize()
+{
+    HRESULT hr = S_OK;
+
+    if (!_pBindings->IsInitialized())
+    {
+        hr = _pBindings->Update();
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindings::Update()
+{
+    return _pBindings->Update();
+}
+
+void XamlBindings::StopTracking()
+{
+    _pBindings->StopTracking();
+}
+
+HRESULT XamlBindings::Loading(IFrameworkElement*, IInspectable*)
+{
+    return Initialize();
+}
+
+HRESULT XamlBindings::DataContextChanged(
+    IFrameworkElement*,
+    IDataContextChangedEventArgs* args)
+{
+    ComPtr<IInspectable> spNewValue;
+    HRESULT hr = args->get_NewValue(&spNewValue);
+    if (SUCCEEDED(hr))
+    {
+        bool fSet;
+        hr = _pBindings->SetDataRoot(spNewValue.Get(), &fSet);
+        if (SUCCEEDED(hr) && fSet)
+        {
+            hr = Update();
+        }
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindings::SubscribeForDataContextChanged(IFrameworkElement* object)
+{
+    return _pBindings->SubscribeForDataContextChanged(object, this);
+}
+
+<# if (Features.xLoad.ExistsInVersion(base.ProjectInfo.TargetPlatformMinVersion)) { #>
+HRESULT XamlBindings::DisconnectUnloadedObject(int connectionId)
+{
+    return _pBindings->DisconnectUnloadedObject(connectionId);
+}
+
+<# } #>
+HRESULT XamlBindings::Connect(int connectionId, IInspectable* target)
+{
+    return _pBindings->Connect(connectionId, target);
+}
+
+HRESULT XamlBindings::ProcessBinding(UINT, boolean* result)
+{
+    *result = false;
+    return E_NOTIMPL;
+}
+
+HRESULT XamlBindings::ProcessBindings(IContainerContentChangingEventArgs* args, int* nextPhase)
+{
+    ComPtr<IInspectable> spItem;
+    HRESULT hr = args->get_Item(&spItem);
+    if (SUCCEEDED(hr))
+    {
+        int itemIndex;
+        hr = args->get_ItemIndex(&itemIndex);
+        if (SUCCEEDED(hr))
+        {
+            UINT phase;
+            hr = args->get_Phase(&phase);
+            if (SUCCEEDED(hr))
+            {
+                hr = _pBindings->ProcessBindings(spItem.Get(), itemIndex, phase, nextPhase);
+            }
+        }
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindings::ResetTemplate()
+{
+    return _pBindings->Recycle();
+}
+
+HRESULT XamlBindings::ProcessBindings(
+    IInspectable* item, int itemIndex, int phase, int* nextPhase)
+{
+    return _pBindings->ProcessBindings(item, itemIndex, phase, nextPhase);
+}
+
+HRESULT XamlBindings::Recycle()
+{
+    return _pBindings->Recycle();
+}
+
+// XamlBindingTrackingBase
+
+XamlBindingTrackingBase::XamlBindingTrackingBase()
+{
+}
+
+void XamlBindingTrackingBase::SetListener(IXamlBindingTracking* pBindings)
+{
+    _pBindingsTrackingWeakRef = pBindings;
+}
+
+HRESULT XamlBindingTrackingBase::PropertyChanged(IInspectable* sender, IPropertyChangedEventArgs* e)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->PropertyChanged(sender, e);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::CollectionChanged(IInspectable* sender, INotifyCollectionChangedEventArgs* e)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->CollectionChanged(sender, e);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::DependencyPropertyChanged(IDependencyObject* sender, IDependencyProperty* prop)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->DependencyPropertyChanged(sender, prop);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::VectorChanged(IInspectable* sender, IVectorChangedEventArgs* e)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->VectorChanged(sender, e);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::MapChanged(IInspectable* sender, <#=Projection(DUI.NamespaceWindowsFoundationCollections)#>::IMapChangedEventArgs<HSTRING>* e)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->MapChanged(sender, e);
+    }
+
+    return hr;
+}
+<# if (ProjectInfo.IsInputValidationEnabled) { #>
+
+HRESULT XamlBindingTrackingBase::ErrorsChanged(IInspectable* sender, IDataErrorsChangedEventArgs* e)
+{
+    HRESULT hr = S_OK;
+
+    if (_pBindingsTrackingWeakRef)
+    {
+        hr = _pBindingsTrackingWeakRef->ErrorsChanged(sender, e);
+    }
+
+    return hr;
+}
+<# } #>
+
+HRESULT XamlBindingTrackingBase::UpdatePropertyChangedListener(
+    INotifyPropertyChanged* obj,
+    ComPtr<INotifyPropertyChanged>& cache,
+    EventRegistrationToken& token)
+{
+    HRESULT hr = S_OK;
+
+    if (cache.Get() && cache.Get() != obj)
+    {
+        hr = cache->remove_PropertyChanged(token);
+        cache.Reset();
+    }
+
+    if (SUCCEEDED(hr) && !cache.Get() && obj)
+    {
+        cache = obj;
+        hr = obj->add_PropertyChanged(
+            Callback<ABI::Windows::UI::Xaml::Data::IPropertyChangedEventHandler>(this, &XamlBindingTrackingBase::PropertyChanged).Get(), &token);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::UpdatePropertyChangedListener(
+    INotifyPropertyChanged* obj,
+    WeakRef& cacheRef,
+    EventRegistrationToken& token)
+{
+    ComPtr<INotifyPropertyChanged> cache;
+    HRESULT hr = cacheRef.As(&cache);
+    if (SUCCEEDED(hr))
+    {
+        if (cache.Get() && cache.Get() != obj)
+        {
+            hr = cache->remove_PropertyChanged(token);
+            cache.Reset();
+            cacheRef.Reset();
+        }
+
+        if (SUCCEEDED(hr) && !cache.Get() && obj)
+        {
+            hr = AsWeak(obj, &cacheRef);
+            if (SUCCEEDED(hr))
+            {
+                hr = obj->add_PropertyChanged(
+                    Callback<ABI::Windows::UI::Xaml::Data::IPropertyChangedEventHandler>(this, &XamlBindingTrackingBase::PropertyChanged).Get(), &token);
+            }
+        }
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::UpdateCollectionChangedListener(
+    INotifyCollectionChanged* obj,
+    ComPtr<INotifyCollectionChanged>& cache,
+    EventRegistrationToken& token)
+{
+    HRESULT hr = S_OK;
+
+    if (cache.Get() && cache.Get() != obj)
+    {
+        hr = cache->remove_CollectionChanged(token);
+        cache.Reset();
+    }
+
+    if (SUCCEEDED(hr) && !cache.Get() && obj)
+    {
+        cache = obj;
+        hr = obj->add_CollectionChanged(
+            Callback<ABI::Windows::UI::Xaml::Interop::INotifyCollectionChangedEventHandler>(this, &XamlBindingTrackingBase::CollectionChanged).Get(), &token);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::UpdateDependencyPropertyChangedListener(
+    IDependencyObject* obj,
+    IDependencyProperty* property,
+    ComPtr<IDependencyObject>& cache,
+    INT64& token)
+{
+    HRESULT hr = S_OK;
+
+    if (cache.Get() && cache.Get() != obj)
+    {
+        ComPtr<IDependencyObject2> cache2;
+        hr = cache.As(&cache2);
+        if (SUCCEEDED(hr))
+        {
+            hr = cache2->UnregisterPropertyChangedCallback(property, token);
+            cache.Reset();
+        }
+    }
+
+    if (SUCCEEDED(hr) && !cache.Get() && obj)
+    {
+        cache = obj;
+
+        ComPtr<IDependencyObject2> obj2;
+        hr = obj->QueryInterface(IID_PPV_ARGS(&obj2));
+        if (SUCCEEDED(hr))
+        {
+            hr = obj2->RegisterPropertyChangedCallback(property,
+                Callback<ABI::Windows::UI::Xaml::IDependencyPropertyChangedCallback>(this, &XamlBindingTrackingBase::DependencyPropertyChanged).Get(), &token);
+        }
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::UpdateDependencyPropertyChangedListener(
+    IDependencyObject* obj,
+    IDependencyProperty* property,
+    WeakRef& cacheRef,
+    INT64& token)
+{
+    HRESULT hr = S_OK;
+
+    ComPtr<IDependencyObject> cache;
+    if (SUCCEEDED(cacheRef.As(&cache)))
+    {
+        if (cache.Get() && cache.Get() != obj)
+        {
+            {
+                ComPtr<IDependencyObject2> cache2;
+                hr = cache.As(&cache2);
+                if (SUCCEEDED(hr))
+                {
+                    hr = cache2->UnregisterPropertyChangedCallback(property, token);
+                }
+            }
+
+            cache.Reset();
+            cacheRef.Reset();
+        }
+
+        if (!cache.Get() && obj)
+        {
+            hr = AsWeak(obj, &cacheRef);
+            if (SUCCEEDED(hr))
+            {
+                ComPtr<IDependencyObject2> obj2;
+                hr = obj->QueryInterface(IID_PPV_ARGS(&obj2));
+                if (SUCCEEDED(hr))
+                {
+                    hr = obj2->RegisterPropertyChangedCallback(property,
+                        Callback<ABI::Windows::UI::Xaml::IDependencyPropertyChangedCallback>(this, &XamlBindingTrackingBase::DependencyPropertyChanged).Get(), &token);
+                }
+            }
+        }
+    }
+
+    return hr;
+}
+
+<# if (ProjectInfo.IsInputValidationEnabled) { #>
+HRESULT XamlBindingTrackingBase::UpdateDataErrorsChangedListener(
+    INotifyDataErrorInfo* obj,
+    ComPtr<INotifyDataErrorInfo>& cache,
+    EventRegistrationToken& token)
+{
+    HRESULT hr = S_OK;
+
+    if (cache.Get() && cache.Get() != obj)
+    {
+        hr = cache->remove_ErrorsChanged(token);
+        cache.Reset();
+    }
+
+    if (SUCCEEDED(hr) && !cache.Get() && obj)
+    {
+        cache = obj;
+        hr = obj->add_ErrorsChanged(Callback<__FIEventHandler_1_Windows__CUI__CXaml__CData__CDataErrorsChangedEventArgs>(this, &XamlBindingTrackingBase::ErrorsChanged).Get(), &token);
+    }
+
+    return hr;
+}
+
+HRESULT XamlBindingTrackingBase::UpdateDataErrorsChangedListener(
+    INotifyDataErrorInfo* obj,
+    WeakRef& cacheRef,
+    EventRegistrationToken& token)
+{
+    HRESULT hr = S_OK;
+
+    ComPtr<INotifyDataErrorInfo> cache;
+    if (SUCCEEDED(cacheRef.As(&cache)))
+    {
+        if (cache.Get() && cache.Get() != obj)
+        {
+            hr = cache->remove_ErrorsChanged(token);
+            cache.Reset();
+            cacheRef.Reset();
+        }
+
+        if (SUCCEEDED(hr) && !cache.Get() && obj)
+        {
+            hr = AsWeak(obj, &cacheRef);
+            if (SUCCEEDED(hr))
+            {
+                hr = obj->add_ErrorsChanged(
+                    Callback<__FIEventHandler_1_Windows__CUI__CXaml__CData__CDataErrorsChangedEventArgs>(this, &XamlBindingTrackingBase::ErrorsChanged).Get(), &token);
+            }
+        }
+    }
+
+    return hr;
+}
+
+<# } #>
+} // namespace
